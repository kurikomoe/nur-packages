name: "Auto update"

permissions:
  actions: write
  contents: write
  statuses: read

on:
  workflow_dispatch:
  schedule:
    # rebuild everyday at 2:51
    # TIP: Choose a random time here so not all repositories are build at once:
    # https://www.random.org/clock-times/?num=1&earliest=01%3A00&latest=08%3A00&interval=5&format=html&rnd=new
    - cron:  '11 22 * * *'

jobs:
  updates:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - uses: fregante/setup-git-user@v2.0.2

      - uses: wimpysworld/nothing-but-nix@main
      - run: df -h

      - name: Install nix
        uses: cachix/install-nix-action@v31
        with:
          nix_path: nixpkgs=channel:nixos-unstable
          extra_nix_config: |
            experimental-features = nix-command flakes
            access-tokens = github.com=${{ secrets.GITHUB_TOKEN }}

      # - uses: nixbuild/nix-quick-install-action@v34
      # - name: Restore and save Nix store
      #   uses: nix-community/cache-nix-action@v7
      #   with:
      #     primary-key: nix-${{ runner.os }}-${{ hashFiles('**/*.nix', '**/flake.lock') }}
      #     restore-prefixes-first-match: nix-${{ runner.os }}-
      #     gc-max-store-size-linux: 5G
      #     purge: true
      #     purge-prefixes: nix-${{ runner.os }}-
      #     purge-created: 0
      #     purge-last-accessed: 0
      #     purge-primary-key: never

      - run: |
          nix-channel --add https://github.com/NixOS/nixpkgs/archive/refs/heads/nixos-unstable.tar.gz nixpkgs
          nix-channel --update

      - name: Show nixpkgs version
        run: nix-instantiate --eval -E '(import <nixpkgs> {}).lib.version'

      # - run: sed -i "s/nix_direnv_manual_reload//g" .envrc
      # - name: Install direnv with Nix
      #   uses: aldoborrero/direnv-nix-action@v2
      #   with:
      #     use_nix_profile: true
      #     nix_channel: nixpkgs

      - name: Prepare Commit Message
        run: |
          echo "Auto update" > commit_message.txt
          echo "" >> commit_message.txt

      - name: Update all deps
        run: |
          nix profile install nixpkgs#nvfetcher
          nix profile install nixpkgs#nix-update

          nix flake update
          if git diff --name-only | grep "flake.lock"; then
             echo "chore: update flake.lock" >> commit_message.txt
             echo "" >> commit_message.txt
          fi

          nvfetcher --changelog nvfetcher_log.txt || true
          if [ -s nvfetcher_log.txt ]; then
             cat nvfetcher_log.txt >> commit_message.txt
             echo "" >> commit_message.txt
             rm nvfetcher_log.txt
          fi

          chmod +x ./nix-update.sh
          ./nix-update.sh

          nix fmt .

      - name: Check for changes
        id: check
        run: |
          if [ -n "$(git status --porcelain)" ]; then
            echo "has_changes=true" >> $GITHUB_OUTPUT
          fi

      - name: Verify Build (Smoke Test)
        if: steps.check.outputs.has_changes == 'true'
        run: |
          echo "Verifying build before commit..."
          nix run github:Mic92/nix-fast-build -- -f .#ci --no-nom --skip-cached --eval-workers 2

      - name: Commit Changes
        uses: EndBug/add-and-commit@v9
        if: ${{ !failure() && steps.check.outputs.has_changes == 'true' }}
        with:
          author_name: github-actions[bot]
          author_email: 41898282+github-actions[bot]@users.noreply.github.com
          committer_name: GitHub Actions
          committer_email: 41898282+github-actions[bot]@users.noreply.github.com
          message_file: "commit_message.txt"
          add: "."

      - name: Setup upterm session
        uses: lhotari/action-upterm@v1
        if: ${{ failure() }}
        with:
          wait-timeout-minutes: 5

      - name: Trigger build all
        uses: peter-evans/repository-dispatch@v4
        if: ${{ !failure()  && steps.check-uncommitted.outputs.has_uncommitted == 'true' }}
        with:
          event-type: build-all-event
